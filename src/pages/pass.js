import Layout from "components/Layout";

const EventPassPage = () => {
  return <Layout>
    <div className="container event-pass-page">This is the event pass page</div>
  </Layout>
}

export default EventPassPage;
